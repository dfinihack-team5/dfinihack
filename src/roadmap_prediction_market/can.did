type Profile = record {
    "name": text;
    "description": text;
    "keywords": vec text;
};

service : {
    "getSelf": () -> (opt Profile) query;
    "updateSelf": (Profile) -> ();
    "getProfile": (text) -> (opt Profile) query;
    "searchProfile": (text) -> (opt Profile) query;
}
